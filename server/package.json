{
  "name": "server",
  "main": "index.js",
  "description": "Mixtube server side component",
  "private": true,
  "engines": {
    "node": ">=7.5.0"
  },
  "scripts": {
    "start": "node $NODE_DEBUG_OPTION index.js",
    "debug": "browser-sync start --no-open --https --proxy 'http://localhost:8080' | npm run start",
    "deploy": "gcloud app deploy  --project mixtube-io-fad1c"
  },
  "devDependencies": {
    "browser-sync": "^2.18.8"
  },
  "dependencies": {
    "compression": "^1.6.2",
    "express": "^4.14.1",
    "got": "^6.7.1"
  }
}
